<pyxserver>
<tests>1</tests>
<processor>

initial_display=''
answer = '''
print('hello world')
'''
preamble = '''
code = """
'''
test_program = '''
"""
import re
# Strip out () so that print( ) call is accepted
# (need to do this so the &gt;&gt; LOG_OUTPUT replacement works)
pattern = re.compile( "print\s*\(([^\)]*)\)")
code = pattern.sub(r"print \\1", code)

code = code.replace("print ", "print &gt;&gt; LOG_OUTPUT, ")
exec(code)
'''
</processor>
</pyxserver>